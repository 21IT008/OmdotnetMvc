@{
    ViewData["Title"] = "Home Page";
    var Name=Context.Session.GetString("Name");
}

<div class="text-center">
    <h1 class="display-4">Welcome @Name</h1>
    <h3 class="text-primary">Total Users @ViewBag.Totalrecords</h3>
    <button class="btn btn-dark rounded-pill"><a class="text-decoration-none text-light" asp-controller="User" asp-action="Register">Click to add more users</a></button>
</div>
